[alembic]
# Slice-00-04 spike migration configuration (SPIKE-EXPERIMENTAL,
# DISPOSITION: DELETE_UNLESS_ADOPTED_BY_00-07).
# The script location is this directory; the database URL is always
# overridden programmatically by the migration helper, so the value below
# is a placeholder that is never used for a real spike run.
script_location = %(here)s
sqlalchemy.url = sqlite:///./unused-placeholder.db

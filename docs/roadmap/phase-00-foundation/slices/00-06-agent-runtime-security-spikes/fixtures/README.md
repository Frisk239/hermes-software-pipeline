# Slice 00-06 authorization goldens (DRAFT r12)

These fixtures pin Host/Custodian JSON shapes. They are test oracles, not runtime authority.
The Executor copies or loads them from `tests/fixtures/security/` and must not invent other schemas.

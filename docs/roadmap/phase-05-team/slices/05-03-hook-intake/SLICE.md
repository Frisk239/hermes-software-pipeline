# Slice 05-03 — hook intake

Status: **READY**. Branch: `feat/slice-05-03-hook-intake`.

## Must

`/card hermes_pipeline {...}` is skipped and, when the runtime is up, submitted as requirement intake. Probe namespace still wins. Other cards and plain chat pass through. Hook never raises.

## Out

Feishu auth, GitHub, rewriting non-pipeline cards.

# Slice 05-11 — verify and record delivery after Candidate

Status: **READY**. Branch: `feat/slice-05-11-verify-deliver`.

## Must

Candidate gate PASS + e2e/reviewer bindings runs VerifyFlow. READY records one namespaced PR. `read` shows `verify_status` and delivery fields. Missing verify bindings is DENIED. No real GitHub.

## Out

Live Chrome, live Codex, merge queue authority.

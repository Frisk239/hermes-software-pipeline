# Slice 05-08 — advance to PRD after intake

Status: **READY**. Branch: `feat/slice-05-08-prd-advance`.

## Must

Accepted `submit` runs one fake PRD Stage. Missing planner is DENIED. `read` stays `status=OPEN` and shows `prd_id` / `prd_status` / `prd_gate`. Survives KernelBridge restart. Controller stays LangGraph-free.

## Out

Architecture/Development, live Codex, new domain status, GitHub.

# Slice 05-02 — project admin and stage bindings

Status: **READY**. Branch: `feat/slice-05-02-project-bind-cli`.

## Must

`pipeline admin` can register a project, admit a member, and bind planner/executor/reviewer/e2e to a runtime+model. Submit no longer auto-admits. Bindings persist in the runtime state root.

## Out

Feishu cards, GitHub App, live Hermes install.

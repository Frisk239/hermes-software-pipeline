# Slice 05-15 — bound executor must write the Candidate

Status: **READY**. Branch: `feat/slice-05-15-real-executor`.

## Must

`fake` executor still writes the fixture file. `opencode` / `codex` must launch the bound runtime and harvest the worktree. Launch failure or empty tree is DENIED. No fixture disguise.

## Out

Live OpenCode/Codex spawn policy change, PRD/Architecture real agents, Feishu.
